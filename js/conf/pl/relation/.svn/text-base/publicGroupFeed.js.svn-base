/**
* 分组详情微博
* 
* @id pl.relation.publicGroupFeed
*/

$Import('comp.relation.groupFeed');

STK.pageletM.register("pl.relation.publicGroupFeed", function($) {
	var node = $.E('pl_relation_publicGroupFeed');
	var that = $.comp.relation.groupFeed(node);
	return that;
});
