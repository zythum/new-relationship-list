/**
 * @author Runshi Wang | runshi@staff.sina.com.cn
 * 
 * 收到的评论、发出的评论、at我的评论、at我的微博 左侧导航
 * @id STK.pl.leftNav.content
 */
$Import('comp.leftNav.content');

STK.pageletM.register("pl.leftNav.content", function($) {
	var node = $.E("pl_leftNav_content");
	var that = $.comp.leftNav.content(node);
	return that;
});