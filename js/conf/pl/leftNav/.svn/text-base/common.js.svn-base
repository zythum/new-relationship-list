/**
 * @author Runshi Wang | runshi@staff.sina.com.cn
 * 
 * 通用左导
 * @id STK.pl.leftNav.common
 */
$Import('comp.leftNav.common');

STK.pageletM.register("pl.leftNav.common", function($) {
	var node = $.E("pl_leftNav_common");
	var that = $.comp.leftNav.common(node);
	return that;
});