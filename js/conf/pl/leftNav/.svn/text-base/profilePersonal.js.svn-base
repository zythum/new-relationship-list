/**
 * @author lianyi@staff.sina.com.cn
 * 
 * 左导（profile页面左导上面菜单层，个人菜单需要处理上传和听歌）
 * @id STK.pl.leftNav.profilePersonal
 */
$Import('comp.leftNav.profilePersonal');
STK.pageletM.register("pl.leftNav.profilePersonal", function($) {
	var node = $.E("pl_leftNav_profilePersonal");
	var that = $.comp.leftNav.profilePersonal(node);
	return that;
});