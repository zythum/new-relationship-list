/**
 * @author Runshi Wang | runshi@staff.sina.com.cn
 * 
 * 用户设置组左侧导航
 * @id STK.pl.leftNav.settings
 */
$Import('comp.leftNav.settings');

STK.pageletM.register("pl.leftNav.settings", function($) {
	var node = $.E("pl_leftNav_settings");
	var that = $.comp.leftNav.settings(node);
	return that;
});