/**
 * @author Runshi Wang | runshi@staff.sina.com.cn
 * 
 * 我的私信左侧导航
 * @id STK.pl.leftNav.message
 */
$Import('comp.leftNav.message');

STK.pageletM.register("pl.leftNav.message", function($) {
	var node = $.E("pl_leftNav_message");
	var that = $.comp.leftNav.message(node);
	return that;
});