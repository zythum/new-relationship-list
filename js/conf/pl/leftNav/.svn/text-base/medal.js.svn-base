/**
 * @author wangliang3
 */
$Import('comp.content.medal');
STK.pageletM.register("pl.leftNav.medal", function($) {
	var node = $.E("pl_leftNav_medal");
	var that = $.comp.content.medal(node,{column:4, autohidden:1});
	return that;
});