/**
* 我的首页Feed组件
* 
* @id pl.content.homeFeed
* @author L.Ming | liming1@staff.sina.com.cn
* @return {Object} 实例 
* @example 
* 暂略
*/

$Import('comp.content.homeFeed');

STK.pageletM.register("pl.content.homeFeed", function($) {
	var opts = {};
	var node = $.E("pl_content_homeFeed");
	var that = $.comp.content.homeFeed(node, opts);
	return that;
});
