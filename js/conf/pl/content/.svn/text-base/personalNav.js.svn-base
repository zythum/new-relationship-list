/**
 *  我的头像模块
 * @id pl.content.myPersonalInfo
* @return {Object} 实例
* @example
 */
$Import('comp.content.personalNav');
STK.pageletM.register("pl.content.personalNav", function($) {
	var node = $.E("pl_content_personalNav");
	var that = $.comp.content.personalNav(node);
	return that;
});