/**
* 
* @id $.pl.content.messageSearch
* @param {Object} node 组件最外节点
* @return {Object} 实例 
* @example 
* demo待完善
*/

$Import('comp.content.messageSearch');


STK.pageletM.register('pl.content.messageSearch',function($){
	var node = $.E('pl_content_messageSearch');
	var that = $.comp.content.messageSearch(node);
	return that;
});
