/**
 * 我的微博页Feed组件
 *
 * @id pl.content.myFeed
 * @author L.Ming | liming1@staff.sina.com.cn
 * @return {Object} 实例
 * @example
 * 暂略
 */

$Import('comp.content.myMoodList');

STK.pageletM.register("pl.content.myMoodList", function($) {
	var opts = {};
	var node = $.E("pl_content_moodlist");
	var that = $.comp.content.myMoodList(node, opts);
	
	return that;
});
