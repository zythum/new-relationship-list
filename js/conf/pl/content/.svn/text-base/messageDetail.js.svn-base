/**
* 
* @id $.pl.content.messageDetail
* @param {Object} node 组件最外节点
* @return {Object} 实例 
* @example 
* demo待完善
*/
$Import('comp.content.messageDetail');


STK.pageletM.register('pl.content.messageDetail',function($){
	var node = $.E('pl_content_messageDetail');
	var that = $.comp.content.messageDetail(node);
	return that;
});
