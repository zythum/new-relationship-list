/**
* 顶托组件
* @id pl.content.top
* @param {Object} node 组件最外节点
* @return {Object} 实例 
* @example 
* 暂略
*/
$Import('comp.content.top');
STK.pageletM.register("pl.content.top",function($){
	var node = $.E("pl_content_top");
	var that = $.comp.content.top(node);
	return that;
});