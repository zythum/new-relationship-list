/**
 * @author ZhouJiequn | jiequn@staff.sina.com.cn
 */
$Import('comp.content.homeInterest');
STK.pageletM.register("pl.content.homeInterest", function($) {
	var node = $.E("pl_content_homeInterest");
	var that = $.comp.content.homeInterest(node);
	return that;
});