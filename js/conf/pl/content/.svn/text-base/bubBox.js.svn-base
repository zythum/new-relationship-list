/**
 */
$Import('comp.content.bubbleTips');

STK.pageletM.register("pl.content.bubBox", function($) {
	var node = $.E("pl_content_bubBox");
	var that = $.comp.content.bubbleTips(node);
	return that;
});