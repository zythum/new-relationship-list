/**
 * common.content.message.upload.getFlash
 * @id STK.
 * @author WK | wukan@staff.sina.com.cn
 * @example
 * 
 */
STK.register('common.content.message.upload.getFlash', function($){
	return function(swfId){
		//$.log('getFlash',$.sizzle('[flashobj="'+swfId+'"]'));
		return $.sizzle('[flashobj="'+swfId+'"]')[0];
	}
});
