/**
* 定义小黄签信道的事件 
* @author yuheng 
*/
$Import('common.listener');
STK.register('common.channel.topTip', function($){
	//@event refresh
	//TV频道的打开事件
	//data
	var eventList = ['refresh','readed'];
	return $.common.listener.define('common.channel.topTip', eventList);
	
});
